return {
  {
    "typicode/bg.nvim",
    lazy = false,       -- load immediately (optional)
    -- Optional: any config you need for bg.nvim
    -- e.g., if it has setup options you can add them here
  },
}
